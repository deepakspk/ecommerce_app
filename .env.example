
# Point this at your machine's LAN IP when testing on a physical device via Expo Go
# (a phone on the same Wi-Fi can't resolve "localhost" as your dev machine).
EXPO_PUBLIC_API_URL=http://localhost:5000/api
