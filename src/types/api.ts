/**
 * Shared TS interfaces mirroring the backend's Mongoose models
 * (01-DOCUMENTATION.md §5 — Data Models). Populated incrementally as each
 * feature prompt needs its own model's shape — kept empty here per Prompt 1's
 * "scaffolding only" scope.
 */
export {};
